# Discord bot framework (zawiera discord.ext.pages)
py-cord>=2.6.1

# HTTP client używany przez py-cord i w utils/discord.py
aiohttp>=3.8.4

# Przetwarzanie obrazów (utils/discord.py - generowanie kolorów z awatarów)
Pillow>=10.0.0

# Zależności dla testów (opcjonalne)
pytest>=8.0.0
pytest-asyncio>=0.21.0
